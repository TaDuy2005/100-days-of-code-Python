# 📅 Day 8: Function Parameters & Caesar Cipher

## 🎓 Course Metadata
- **Course:** 100 Days of Code Python  
- **Difficulty:** ⭐

## 📌 Overview
Day 8 focused on making functions more flexible by passing input through parameters. The session also introduced positional and keyword arguments, along with practical list utilities, all applied in building a classic encryption program using the Caesar Cipher technique.

## 🧠 Key Concepts
- Defining functions with parameters
- Passing data using positional arguments
- Using keyword arguments for clarity
- Reusing functions with different inputs
- Rounding numbers upward with `ceil()`
- Finding element positions using `index()`
- Shifting characters for basic encryption logic

## 🛠️ Functions & Features Practiced
- Functions with one or multiple parameters
- Positional vs keyword arguments
- `math.ceil()`
- `list.index()`
- Conditional logic inside functions
- Looping through strings and lists
- Modulo (`%`) to control shift limits

## 🎯 Project of the Day

### 🔐 Caesar Cipher
A text-based encryption and decryption program that shifts letters by a user-defined amount.

**Highlights:**
- Accepted user input for text, shift value, and direction
- Used a reusable function to handle both encoding and decoding
- Applied list indexing to shift characters accurately
- Preserved non-alphabet characters in the final output
- Allowed users to restart the program without rerunning the script

This project reinforces function parameters, list indexing, and loop-based text processing.

## ✅ Outcomes
- Learned how to pass and manage data using function parameters
- Understood the difference between positional and keyword arguments
- Practiced using built-in math and list methods
- Built a complete encryption tool using core Python logic
- Strengthened confidence in writing reusable, input-driven functions
